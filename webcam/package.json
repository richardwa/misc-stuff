{
  "name": "webcam-viewer",
  "version": "1.0.0",
  "description": "Webcam Viewer",
  "scripts": {
    "prebuild": "rimraf docs",
    "build": "parcel build app/index.html --public-url '.' --out-dir docs",
    "preserve": "rimraf target",
    "serve": "node devserver/server.js",
    "test": "tsc --project tsconfig.test.json",
    "check": "tsc"
  },
  "devDependencies": {
    "cross-fetch": "^3.0.4",
    "express": "^4.17.1",
    "http-proxy-middleware": "^1.0.4",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "jss": "^10.1.1",
    "parse-apache-directory-index": "^1.2.1",
    "preact": "^10.4.1"
  }
}
