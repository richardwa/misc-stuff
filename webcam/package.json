{
  "name": "mathworksheet",
  "version": "1.0.0",
  "description": "Math worksheet generator https://richardwa.github.io/mathworksheet/",
  "main": "index.html",
  "scripts": {
    "prebuild": "rimraf docs",
    "build": "parcel build app/index.html --public-url '.' --out-dir docs",
    "preserve": "rimraf target",
    "serve": "parcel app/index.html --out-dir target --port 8000",
    "check": "tsc"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.2"
  },
  "dependencies": {      
    "jss": "^10.1.1",
    "preact": "^10.4.1"
  }
}